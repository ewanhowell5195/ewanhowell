@import "buttons.styl"

.container
  max-width: var(--content-width)
  margin: 0 auto
  padding: 20px 20px
  display: flex
  justify-content: center
  align-items: center
  min-height: calc(100vh - var(--header-height) * 2 - 40px)